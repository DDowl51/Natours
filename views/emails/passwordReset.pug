extends baseEmail

block content
  p= `Hi ${firstName}`
  p= `Forgot your password? Submit a PATCH request with your new password and passwordConfirm to: ${data.resetUrl}.`
  p Link above will expire in 10 minutes!
  p (Website for this action not implement yet.)
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${data.resetUrl}`, target='_blank') Reset your password
  p= `Or paste the link ${data.resetUrl} to browser manually.`
    

  p If you did't forget your password, please ignore this email!;
  p - Jonas Schmedtmann, CEO